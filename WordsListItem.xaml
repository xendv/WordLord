<UserControl x:Class="WordLord.WordsListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WordLord"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="-4,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding ElementName=Word, Path=WordFull}" Grid.Column="0" FontSize="20" Margin="20"></TextBox>
        <TextBox Grid.Column="1" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">Изменить</TextBox>
        <Button Grid.Column="3" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">Удалить</Button>
    </Grid>
</UserControl>
